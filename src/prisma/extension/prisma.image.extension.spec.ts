// TODO - $extends is not working (prismock issue)
describe('PrismaImageExtension', () => {
  beforeEach(async () => {
    jest.clearAllMocks();
  });

  it('should throw error during upload and delete image', async () => {
    // Add your test here
  });
});
