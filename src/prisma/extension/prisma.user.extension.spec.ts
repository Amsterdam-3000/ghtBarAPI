// TODO - $extends is not working (prismock issue)
describe('PrismaUserExtension', () => {
  beforeEach(async () => {
    jest.clearAllMocks();
  });

  it('should throw error with wrong credentials', async () => {
    // Add your test here
  });
});
