// TODO - $extends is not working (prismock issue)
describe('PrismaItemExtension', () => {
  beforeEach(async () => {
    jest.clearAllMocks();
  });

  it('should get with image', async () => {
    // Add your test here
  });
});
